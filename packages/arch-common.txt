# cache22 — arch family base packages (arch-kde and arch-server share these).
#
# These variants are built on the archlinux/archlinux base image and
# pull from ALHP (x86-64-v3 optimized rebuilds of stock Arch), stock
# Arch (fallback for anything not in ALHP), LizardByte's pacman repo
# (sunshine), the cmspam/* repos for bootc, qemu, virglrenderer,
# gamescope, and the in-image cache22-aur repo (AUR packages we build
# ourselves with v3 flags during the image build). DKMS-built kernel
# modules (nvidia-open, r8125, broadcom-wl, xone) compile at image-
# build time against linux-headers.
#
# Independent of the cachy family — designed to keep working if the
# cachy-* files get deleted entirely.

# ─── Base system ──────────────────────────────────────────
base
base-devel
linux-firmware
linux-firmware-amdgpu
sof-firmware
amd-ucode
intel-ucode
sudo
which
inetutils
iptables
nftables
less
diffutils
perl
python
man-db
man-pages
texinfo
logrotate
lsb-release
sysfsutils
usbutils
pciutils

# ─── Kernel + DKMS-built out-of-tree modules ──────────────
# Mainline `linux` (7.x). nvidia-open-dkms and r8125-dkms compile
# against linux-headers via the dkms alpm hook during pacman -S,
# producing /usr/lib/modules/<kver>/extra/*.ko baked into the image.
# broadcom-wl-dkms is also DKMS.
linux
linux-headers
nvidia-open-dkms
r8125-dkms

# ─── GPU userland (headless-safe) ─────────────────────────
# Desktop-only bits (nvidia-settings, egl-wayland, xf86-video-amdgpu,
# nvidia-prime, switcheroo-control, mesa-utils, vulkan-tools, lib32-*)
# live in arch-kde.txt so the server image stays X11/Wayland-free.
nvidia-utils
opencl-nvidia
libva-nvidia-driver
mesa
vulkan-icd-loader
vulkan-radeon
vulkan-intel
vulkan-virtio       # venus driver — Vulkan inside virtio-gpu guests
intel-media-driver
intel-compute-runtime    # OpenCL + Level Zero on Intel iGPU/dGPU
libva-utils              # vainfo
vdpauinfo
gst-plugin-va

# ─── bootc + immutable infrastructure ─────────────────────
bootc
dracut
ostree
composefs
skopeo

# ─── Bootloader + UKI ─────────────────────────────────────
efibootmgr
sbctl
sbsigntools
systemd-ukify

# Wraps signing tools (sbsign, dracut) at build time so embedded
# wall-clock timestamps become deterministic. Tiny library, harmless
# at runtime.
libfaketime

# ─── Storage / filesystems ────────────────────────────────
cryptsetup
device-mapper
lvm2
mdadm
dosfstools
e2fsprogs
exfatprogs
btrfs-progs
xfsprogs
f2fs-tools
nfs-utils
samba
smbclient
smartmontools

# ─── Network ──────────────────────────────────────────────
networkmanager
networkmanager-openvpn
modemmanager
iwd
wpa_supplicant
dnsmasq
bind
ethtool
nss-mdns
usb_modeswitch
wireless-regdb
ufw
wireguard-tools

# ─── Bluetooth ────────────────────────────────────────────
bluez
bluez-libs
bluez-utils
bluez-hid2hci
bluez-obex

# ─── Audio (PipeWire) ─────────────────────────────────────
pipewire
pipewire-pulse
pipewire-alsa
pipewire-jack
wireplumber
alsa-firmware
alsa-plugins
alsa-utils

# ─── Power management ─────────────────────────────────────
power-profiles-daemon
cpupower
upower

# ─── Hardware enablement ──────────────────────────────────
dmidecode
hdparm
hwdetect
hwinfo
lsscsi
mtools
sg3_utils
lm_sensors
bolt
fwupd
fprintd
broadcom-wl-dkms

# ─── Virtualization (qemu-base; KDE adds qemu-desktop + virt-manager) ─
qemu-base
libvirt
virglrenderer
swtpm
edk2-ovmf
iproute2

# ─── Containers ───────────────────────────────────────────
podman
podman-compose
docker
docker-compose
docker-buildx
distrobox
incus
incus-tools
lxc
crun
slirp4netns
fuse-overlayfs
aardvark-dns
netavark

# ─── Flatpak (bazaar GUI storefront in arch-kde.txt) ──────
flatpak

# ─── Common userland CLI ──────────────────────────────────
bash-completion
git
openssh
rsync
wget
curl
jq
go-yq
tree
ncdu
socat
whois
lsof
bat
fzf
iperf3
python-pip
python-virtualenv
python-pipx
zstd
7zip
ripgrep
fd
btop
fastfetch
nano
nano-syntax-highlighting
vim
micro
unrar
unzip
zip
unarchiver
pacman-contrib
pkgfile
rebuild-detector
reflector
pv

# ─── Ops / admin / monitoring ─────────────────────────────
tmux
htop
iotop
iftop
nethogs
bmon
strace
tcpdump
nmap
mtr
sysstat
inxi

# ─── TPM2 ─────────────────────────────────────────────────
tpm2-tools
tpm2-tss
